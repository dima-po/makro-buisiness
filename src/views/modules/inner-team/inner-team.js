const card = document.querySelector('.makro-team__card');
const cardDetails = document.querySelector('.makro-team__card-details');

if (cardDetails) {
  // cardDetails.setAttribute('style', `width:${card.offsetWidth * 3}px`)
}
